@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
body {
  background-color: #ededed;
  margin: 0;
  font-family: "Open Sans", arial, sans-serif;
}

.top_bar {
  background-color: #548fff;
  padding: 10px;
  color: #ededed;
  text-align: center;
  font-size: 20px;
  letter-spacing: 1px;
}

.content_section {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  align-content: center;
  height: 80vh;
}
.content_section .search {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.content_section .search .logo {
  display: block;
  margin-bottom: 50px;
}
.content_section .search .search_bar {
  display: flex;
  flex-direction: row;
}
.content_section .search .search_bar .input_summoner {
  width: 450px;
  outline: none;
  padding: 16px 32px;
  border-radius: 1000px;
  border: 0;
  font-size: 20px;
  font-weight: bold;
}
.content_section .search .search_bar .input_summoner::placeholder {
  font-weight: normal;
  color: #a5a5a5;
}
.content_section .search .search_bar .searchSummoner {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 45px;
  height: 45px;
  margin: 5px;
  background-color: #548fff;
  color: white;
  border-radius: 1000px;
  transform: translateX(-56px);
  font-size: 20px;
  transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
}
.content_section .search .search_bar .searchSummoner .search_btn_icon.fa-spinner {
  animation-name: spin;
  animation-duration: 2000ms;
  animation-iteration-count: infinite;
  animation-timing-function: linear;
}
.content_section .search .search_bar .searchSummoner:hover {
  cursor: pointer;
  box-shadow: 0 10px 20px rgba(47, 47, 47, 0.19), 0 6px 6px rgba(47, 47, 47, 0.23);
}
.content_section .search .query_message {
  background-color: #ff6060;
  color: #ededed;
  padding: 5px 12px;
  margin-top: 24px;
  border-radius: 1000px;
}
.content_section .search .note {
  color: #a5a5a5;
}
.content_section .matchHistory .history-tab {
  background-color: #ff6060;
  display: flex;
  margin-bottom: 10px;
}
.content_section .matchHistory .history-tab.win {
  background-color: #548fff;
}
.content_section .matchHistory .info {
  margin: 10px 20px;
  color: #ededed;
}
.content_section .matchHistory .back_btn {
  background-color: #212121;
  padding: 10px 20px;
  border-radius: 1000px;
  display: inline-block;
  color: #ededed;
  cursor: pointer;
}

/*# sourceMappingURL=main.csscd...map */
